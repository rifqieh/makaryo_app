class Kategori {
  var _color, _kategoriName, _percentage;

  Kategori(this._color, this._kategoriName, this._percentage);

  getColor() => this._color;
  getKategoriName() => this._kategoriName;
  getPercentage() => this._percentage;
}
